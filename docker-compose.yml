networks:
  dev:

services:
  nginx:
    image: nginx:stable-alpine
    ports:
      - "80:80"
      - "80:443"
    volumes:
      - './nginx.conf:/etc/nginx/nginx.conf'
    
    depends_on:
      - backend
      - frontend

  backend:
    build: 
      context: ./backend
    networks:
      - dev

  frontend:
    build: 
      context: ./frontend
    networks:
      - dev
    deploy:
      resources:
        limits:
          memory: 1G 
